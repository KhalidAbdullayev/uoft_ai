# uoft_ai
AI helper for UofT students. Uses open-source models, along with web crawlers to collect data.
To use AI, simply run the main.py file. Note that embeddings.py and site_test.py were used to collect the needed database. However, as these files are already built, and two Python programs described above are not used in this AI directly, there is no need to run these programs.